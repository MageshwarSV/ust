Get-ChildItem -Recurse -File | Where-Object { $_.FullName -notmatch '\\(\.git|node_modules|venv|dist|build)\\' } | ForEach-Object {
    "--- FILE: $($_.FullName) ---"
    Get-Content $_.FullName -Raw
    "`n"
} | Out-File -FilePath project_code.txt -Encoding utf8
